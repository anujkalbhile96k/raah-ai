[project]
name = "raah-ai"
version = "0.1.0"
requires-python = ">=3.12,<3.15"

dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "networkx>=3.2.0",
    "requests>=2.31.0",
    "httpx>=0.27.0",
    "pydantic>=2.6.0"
]

[tool.vercel]
entrypoint = "backend.main:app"
